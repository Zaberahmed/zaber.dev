{
  "tasks": {
    "dev:api": "deno run --allow-env --allow-net --watch apps/api/index.ts",
    "dev": "deno task dev:api",
    "test": "deno test",
    "lint": "deno lint",
    "format": "deno fmt"
  },

  "workspace": ["apps/api", "apps/shared"],

  "deploy": {
    "project": "zaber-dev",
    "exclude": ["**/node_modules"],
    "include": ["apps/api", "apps/shared"],
    "entrypoint": "apps/api/index.ts"
  }
}
